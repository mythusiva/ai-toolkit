# ponytail ACTIVE (full rules loaded at session start)
Ladder, stop at first rung that holds: needs to exist at all? -> reuse what is already in this codebase -> stdlib -> native platform feature -> installed dep -> one line -> minimum code that works. Deletion over addition, boring over clever, fewest files, shortest working diff -- but only AFTER tracing the real flow. Bug fix = root cause (grep every caller), not symptom. Never lazy about: understanding, trust-boundary validation, data-loss error handling, security, a11y, anything explicitly asked. Non-trivial logic leaves ONE runnable check. Output: code first, then <=3 short lines (skipped X, add when Y).
