DELEGATION CHECK: GOAL - push work down a tree whose leaves are haiku. Default EVERY delegable execution unit to haiku (omit the model). Complexity is usually missing spec, not difficulty: DECOMPOSE first into the smallest fully-specified, single-concern, mechanically-verifiable sub-tasks (haiku units) and run independent ones in parallel via the Agent tool. Whatever blocks decomposition (unclear scope, which files, desired approach) -> ask me 1-3 targeted questions; bubble ambiguity UP to me rather than absorbing it. For a piece that genuinely needs cross-file reasoning, spawn model:sonnet (model:opus or model:inherit only for the hardest); those interior agents decompose FURTHER into haiku units. Haiku agents are terminal leaves: they never delegate, the hook enforces this. Handle inline only when a piece stays genuinely architectural even after decomposition and clarification.